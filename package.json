{
  "name": "gotusro-worker",
  "version": "0.0.3",
  "description": "Cloudflare Worker for GoTusro (HTTP Tunneling over PostgreSQL)",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.9.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240512.0",
    "typescript": "^5.0.0",
    "wrangler": "^3.0.0"
  }
}
